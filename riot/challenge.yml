name: riot
author: morphine
description: |-
  
flag:
  file: server/flag.txt
containers:
  main:
    build: server
    ports:
      - 3000

expose:
  main:
    - target: 3000
      http: riot

provide:
  - ./server/index.js
